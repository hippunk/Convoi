;Exemple : ask drone 0 [ add-intention (word "check-pos-intention " [z_zone] of  patch 10 10 10) "check-pos-done"]  
to check-pos-intention [obj]
  ;calcul du chemin si celui-ci ne correspond pas à l'intention
  if intention-courante != (word "check-pos " obj)
  [

    set intention-courante (word "check-pos " obj)
    drone-astar-to-zone obj false
  ]
  
end

to-report check-pos-done
  ;revoie true si on est dans la zone à checker
  if finished?[
    set intention-courante ""
  ]
  report finished?

end


<<<<<<< HEAD
to strat-kill-pos [pos]
  set objectif pos
  ;set fire? true

  ;+Autorisation de tir une fois dans la zone ?
end



=======
>>>>>>> a161f85ba7a76ebde840dd067eb11d8dfe97a86c
to kill-pos-intention [obj]
  ;calcul du chemin si celui-ci ne correspond pas à l'intention
  if intention-courante != (word "kill-pos " obj)
  [
    set fire? true
    set intention-courante (word "kill-pos " obj)
    drone-astar-to-zone obj false
  ]
  
end

to-report kill-pos-done
  ;revoie true si on est dans la zone à checker
  if finished?[
    set fire? false
    set intention-courante ""
  ]
  report finished?

end


to check-path-intention
  ;calcul du chemin si celui-ci ne correspond pas à l'intention
  if intention-courante != (word "check-path") ;Passage en paramètre du convoi suivi ?
  [
    set fire? true
    set intention-courante (word "chekc-path") ; 
    ;drone-astar-to-zone obj false Calcul du chemin ? Récupération ?
  ]
  
end

to-report check-path-done
  ;revoie true si on est dans la zone à checker
  if finished?[  
    set intention-courante ""
  ]
  report finished?

end